import { LastNameFormatPipe } from './last-name-format.pipe';

describe('LastNameFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new LastNameFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
